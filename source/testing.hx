import haxe.iterators.DynamicAccessKeyValueIterator;

class Testing {
    function d(ball:haxe.ds.List) {
        catch (ball) {
            dribble();
        }
    }

    function dribble() {
        var down:DynamicAccessKeyValueIterator = null;
        throw (ball, down);
    }
}